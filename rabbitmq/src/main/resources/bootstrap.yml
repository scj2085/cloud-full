spring:
  application:
    name: rabbitmq
  rabbitmq:
    host: localhost
    port: 5672
    username: springcloud
    password: 123456
  cloud:
    stream:
      bindings:
        input:
          content-type: application/json      
		  destination: output
		output:
		  destination: input  